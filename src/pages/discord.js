import React from 'react';
import Layout from "../components/layout";

function DiscordPage(props) {
    return <Layout>
        <div>
            <h1>discord render</h1>
        </div>
    </Layout>
}

export default DiscordPage;
